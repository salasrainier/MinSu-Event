{{> head}}
<body class="bg-gradient-to-br from-green-50 via-emerald-50 to-green-100 flex items-center justify-center min-h-screen p-4">

  <div class="w-full max-w-md bg-white rounded-3xl shadow-2xl overflow-hidden border border-green-100 animate-fadeIn">
    
    <!-- Premium Banner -->
    <div class="bg-gradient-to-r from-green-700 to-emerald-600 text-white text-center py-8 px-6 relative overflow-hidden">
      <div class="absolute inset-0 opacity-5"></div>
      <div class="flex items-center justify-center gap-3 mb-3 relative z-10">
        <img src="/uploads/OIP.jpg" alt="MSU Logo" class="w-10 h-10 rounded-full object-cover" />
        <h1 class="text-3xl font-bold tracking-tight">MinSU Events</h1>
      </div>
      <p class="text-sm text-green-100 relative z-10 font-medium">Mindoro State University Event Management</p>
    </div>

    <div class="p-8">
      <h2 class="text-3xl font-bold mb-2 text-center text-green-900">Welcome Back</h2>
      <p class="text-center text-gray-600 text-sm mb-8">Sign in to manage your events</p>

      <div id="notif" class="mb-6 hidden p-4 rounded-xl text-sm font-medium transition-all duration-300"></div>

      <form id="loginForm" method="POST" action="/auth/login" novalidate class="space-y-5">
        <!-- Email Field -->
        <div class="group">
          <label for="loginEmail" class="block text-sm font-semibold text-green-900 mb-2.5">Email Address</label>
          <div class="relative">
            <input type="email" id="loginEmail" name="email" placeholder="you@example.com" autocomplete="email"
                   class="w-full px-4 py-3.5 pl-11 border-2 border-green-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent transition-all duration-200 bg-white hover:border-green-300" required />
            <svg class="absolute left-4 top-3.5 w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
            </svg>
          </div>
        </div>

        <!-- Password Field -->
        <div class="group">
          <label for="loginPassword" class="block text-sm font-semibold text-green-900 mb-2.5">Password</label>
          <div class="relative">
            <input type="password" id="loginPassword" name="password" placeholder="••••••••" autocomplete="current-password"
                   class="w-full px-4 py-3.5 pl-11 border-2 border-green-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-green-400 focus:border-transparent transition-all duration-200 bg-white hover:border-green-300 pr-12" required />
            <svg class="absolute left-4 top-3.5 w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
            </svg>
            <button type="button" id="loginTogglePass" class="absolute right-3 top-3 text-green-600 hover:text-green-800 hover:scale-110 transition-all duration-150 p-1.5 rounded-lg hover:bg-green-50 cursor-pointer active:scale-95">
              <!-- Eye open icon -->
              <svg class="w-5 h-5 eye-open" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <circle cx="12" cy="12" r="3" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                <path stroke-width="2" stroke-linecap="round" stroke-linejoin="round" d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/>
              </svg>
              <!-- Eye closed with slash icon -->
              <svg class="w-5 h-5 eye-closed" fill="none" stroke="currentColor" viewBox="0 0 24 24" style="display:none;">
                <path stroke-width="2" stroke-linecap="round" stroke-linejoin="round" d="M3.98 8.223A10.477 10.477 0 001.934 12C3.226 16.338 7.244 19.5 12 19.5c.993 0 1.953-.138 2.863-.395M6.228 6.228A10.45 10.45 0 0112 4.5c4.756 0 8.773 3.162 10.065 7.498a10.523 10.523 0 01-4.293 5.774M6.228 6.228L3 3m3.228 3.228l3.65 3.65m7.894 7.894L21 21m-3.228-3.228l-3.65-3.65m0 0a3 3 0 10-4.243-4.243m4.242 4.242L9.88 9.88"/>
              </svg>
            </button>
          </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="w-full bg-gradient-to-r from-green-600 to-emerald-600 hover:from-green-700 hover:to-emerald-700 text-white font-bold py-3.5 rounded-xl transition-all duration-200 transform hover:-translate-y-0.5 hover:shadow-xl active:translate-y-0 flex items-center justify-center gap-2 text-base mt-7">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 15l-3-3m0 0l3-3m-3 3h8M3 12a9 9 0 1118 0 9 9 0 01-18 0z" />
          </svg>
          Sign In
        </button>
      </form>

      <!-- Divider -->
      <div class="relative my-7">
        <div class="absolute inset-0 flex items-center">
          <div class="w-full border-t border-green-200"></div>
        </div>
        <div class="relative flex justify-center text-sm">
          <span class="px-2 bg-white text-gray-500 font-medium">New here?</span>
        </div>
      </div>

      <!-- Register Link -->
      <p class="text-center text-gray-700">
        <a href="/auth/register" class="text-green-600 font-bold hover:text-green-700 transition duration-200">Create Account</a>
      </p>
    </div>

    <div class="bg-green-50 border-t border-green-100 py-4 text-center text-gray-500 text-xs">
      {{> footer}}
    </div>
  </div>

  <!-- Success Modal -->
  <div id="successModal" class="fixed inset-0 flex items-center justify-center bg-black/60 hidden z-50 backdrop-blur-sm">
    <div class="bg-white rounded-3xl shadow-2xl p-8 max-w-sm mx-4 animate-slideUp">
      <div class="flex justify-center mb-4">
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center text-3xl animate-bounce">
          ✓
        </div>
      </div>
      <h3 class="text-2xl font-bold text-center text-green-900 mb-2">Welcome!</h3>
      <p class="text-center text-gray-600 mb-4">Redirecting to dashboard...</p>
      <div class="flex justify-center">
        <div class="inline-flex items-center gap-1.5">
          <div class="w-2 h-2 bg-green-600 rounded-full animate-bounce" style="animation-delay: 0s"></div>
          <div class="w-2 h-2 bg-green-600 rounded-full animate-bounce" style="animation-delay: 0.2s"></div>
          <div class="w-2 h-2 bg-green-600 rounded-full animate-bounce" style="animation-delay: 0.4s"></div>
        </div>
      </div>
    </div>
  </div>

<script>
// Just handle password toggle, let form submit normally
const loginToggleBtn = document.getElementById("loginTogglePass");
const loginPasswordInput = document.getElementById("loginPassword");
const loginForm = document.getElementById("loginForm");

if (loginToggleBtn) {
  loginToggleBtn.addEventListener("click", (e) => {
    e.preventDefault();
    const isPassword = loginPasswordInput.type === "password";
    loginPasswordInput.type = isPassword ? "text" : "password";
    
    const eyeOpen = loginToggleBtn.querySelector(".eye-open");
    const eyeClosed = loginToggleBtn.querySelector(".eye-closed");
    
    if (isPassword) {
      eyeOpen.style.display = "none";
      eyeClosed.style.display = "block";
    } else {
      eyeOpen.style.display = "block";
      eyeClosed.style.display = "none";
    }
    
    loginToggleBtn.classList.add("scale-110");
    setTimeout(() => loginToggleBtn.classList.remove("scale-110"), 200);
  });
}

// Let the form submit normally via POST
if (loginForm) {
  loginForm.addEventListener("submit", function(e) {
    // Normal form submission - browser will handle it
    // This will POST to /auth/login and let server handle redirect
  });
}
</script>

<style>
@keyframes fadeIn { from { opacity: 0; transform: translateY(-10px); } to { opacity: 1; transform: translateY(0); } }
@keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
@keyframes bounce { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-8px); } }
@keyframes gridFade { 0% { opacity: 0.1; } 50% { opacity: 0.05; } 100% { opacity: 0.1; } }

.animate-fadeIn { animation: fadeIn 0.5s ease-out forwards; }
.animate-slideUp { animation: slideUp 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }
.animate-bounce { animation: bounce 1s infinite; }
.bg-grid { background-image: linear-gradient(rgba(255,255,255,.1) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.1) 1px, transparent 1px); background-size: 50px 50px; }
</style>
</body>
